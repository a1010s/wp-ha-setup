{% set previous_group = "" %}
{% for host in groups['all'] %}
{% if hostvars[host]['ansible_hostname'] != previous_group %}
[{{ hostvars[host]['ansible_hostname'] }}]
{% set previous_group = hostvars[host]['ansible_hostname'] %}
{% endif %}
{{ host }} ansible_hostname={{ hostvars[host]['ansible_hostname'] }}
{% endfor %}
